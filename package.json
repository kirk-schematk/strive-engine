{
  "name": "strive-engine",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "deploy": "node build.js && wrangler deploy",
    "validate:lessons": "node tools/validate-lesson.js"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "wrangler": "^4"
  }
}
